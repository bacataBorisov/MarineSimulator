# Open questions

- Does the target external reader accept the current `MWV`, `MWD`, `HDG`, `HDT`, `VTG`, `RMC`, UDP, and TCP behavior without additional interoperability fixes?
- Is the `BoatMapPreview.swift` preview crash caused by preview infrastructure only, or by a real runtime issue that should be fixed before more dashboard work?
- Should the next weather iteration add current/drift and more environmental values, or should weather stop at V1 while the project returns to protocol fidelity work?
